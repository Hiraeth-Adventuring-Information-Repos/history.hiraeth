1. **Modify `index.html` to add close icon**: Update `#sidebar-toggle` to include an `icon-close` (X) SVG alongside the existing `icon-menu` (hamburger) SVG. Set the `icon-close` to `hidden` by default. Add `aria-controls="sidebar"` to the button for proper screen reader association. I will use `replace_with_git_merge_diff` to modify this file.
2. **Refactor sidebar toggle logic**: Replace the repetitive sidebar state toggling logic in `index.html` with a unified `toggleSidebar` function. This function will handle adding/removing classes, `aria-expanded`, `inert`, focusing the search input, AND toggling the visibility of `icon-menu` and `icon-close`. Update the toggle button click, document click, link click, and Escape key listeners to use this function. I will use `replace_with_git_merge_diff` to modify this file.
3. **Verify DOM logic**: I will use `run_in_bash_session` to check with `cat` or `grep` that the HTML and JS logic updates were applied correctly.
4. **Create a journal entry**: Use `run_in_bash_session` to echo a new journal entry into `.jules/palette.md` to document the reusable pattern for accessible, stateful sidebar toggles.
5. **Run tests**: I will use `run_in_bash_session` to run all python UI tests, such as `python3 test_delegation.py`, `python3 test_empty_search.py`, `python3 test_kbd_shortcut.py` to ensure that no regression is introduced.
6. **Pre-commit checks**: Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
7. **Submit**: Create a pull request using the standard Palette format.
